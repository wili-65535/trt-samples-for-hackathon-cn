# Generated by main.py: two exported ONNX graphs (~650 MB each) and two engines (~480 MiB each).
# They are caches -- delete them and the next run rebuilds. Never commit them.
model-gpt2-step*.onnx
model-gpt2-step*.trt
